spring:
  application:
    name: speakview
  profiles:
      active: local
      group:
        local: local
        prod: prod

soniox:
  api-key: ${SONIOX_API_KEY}

openapi:
  api-key: ${GPT_API_KEY}
  model: gpt-4o-mini